{
  "name": "kestovar-engine",
  "version": "4.3.8",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "db:migrate": "wrangler d1 migrations apply signalcore-engine-db"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  }
}